[package]
name = "plato-tile-api"
version = "0.1.0"
edition = "2021"
description = "Tile processing API — validate, score, store, search, rank with zero external deps"
license = "MIT"
repository = "https://github.com/SuperInstance/plato-tile-api"
readme = "README.md"
